<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
    <context-param>
        <param-name>javax.faces.CONFIG_FILES</param-name>
        <param-value>/faces-config.xml</param-value>
    </context-param>
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>

    <!-- 1. Lock down the /prof/* URL path -->
    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Professor Secured Pages</web-resource-name>
            <url-pattern>/prof/*</url-pattern>
        </web-resource-collection>
        <!-- 2. Require the PROFESSOR role to access it -->
        <auth-constraint>
            <role-name>PROFESSOR</role-name>
        </auth-constraint>
    </security-constraint>

    <!-- 3. Declare the security roles used in the application -->
    <security-role>
        <role-name>PROFESSOR</role-name>
    </security-role>

</web-app>